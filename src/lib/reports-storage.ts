/**
 * @deprecated Use Convex via `api.reports` and types from `@/lib/reports`.
 * Kept for imports that expect StoredReport from this path.
 */
export type { StoredReport, ReportStatus, ReportCategory } from '@/lib/reports'
